CREATE TABLE bot (
id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, 
user_id  INTEGER REFERENCES user(id), 
bot_name TEXT NOT NULL ON DELETE CASCADE, 
created_date TIMESTAMPTZ DEFAULT now() NOT NULL,
);